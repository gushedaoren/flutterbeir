import 'package:flutter/material.dart';

class PageVideoHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('英语动画'),),
      body: Center(
        child: Text('动画'),
      ),
    );
  }
}