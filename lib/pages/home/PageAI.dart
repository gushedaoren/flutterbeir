import 'package:flutter/material.dart';

class PageAI extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('智能'),),
      body: Center(
        child: Text('智能'),
      ),
    );
  }
}